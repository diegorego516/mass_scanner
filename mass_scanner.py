import os
import sys
import nmap

print("\nInitializing Mass Scanner...")
input("Choose From options ('1-5'): ")
input("1. Scan for Open Ports ('1-65535'): ")
input("2. Generate a detailed vulnerability report log? ('Y/N'): ")
input("3. Send in the detailed vulnerability report log? ('Y/N'): ")
input("4. Ensure that the Mass Scanner scans for Open_Ports properly and regularly from time to time in order to avoid potential leaks on your system? (Y/N):  ")
input("5. Exit: ")
sys.exit(0)
print("exiting...")
System_On_Exit("Clear")
