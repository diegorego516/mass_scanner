import os
import sys
import nmap

def port():

        if port in nmap:

                def nmap(os, sys):
# Aqui é onde nós criamos a função do mass_scanner

                        criar = mass_scanner()
def mass_scanner():

        if mass_scanner in ports(nmap):

                mass_scan(in_ports);

def scan():
                nmap = scan in open_ports("1-65535");

def opção():

                input = opção ("Escolha a opção de ('1-5'): ")
                print ("inicializando o mass scanner...")
                input = opção("1-5")
                input = opção("1. Escanear Por Portas Abertas (1-65535): ")
                input = opção("2. Verificar se há vulnerabilididades em sistemas operacionais: ")
                input = opção("3. Gerar um relatório de log ('Y/N'): ")
                input = opção("4. Enviar o relatório detalhado de vulnerabilidades para especialistas de cibersegurança: ")
                input = opção("5. Sair: ")
                sys.exit(0)
                print("Saindo...")
